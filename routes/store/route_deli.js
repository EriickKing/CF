const express = require("express");
const router = express.Router();
const joi = require("joi");
const { Delivery } = require("../../models/store/model_delivery");

router.post("/create", (req, res) => {
  
})

module.exports = router;